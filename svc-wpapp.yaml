apiVersion: v1
kind: Service
metadata:
  labels:
    app: wpapp
    app.kubernetes.io/part-of: wpapp
  name: wpapp
spec:
  ports:
  - name: tcp-wpapp
    port: 8080
    protocol: TCP
    targetPort: tcp-wpapp
  selector:
    app: wpapp
  type: ClusterIP
